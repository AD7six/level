"""
${TITLE}

Description:
- Briefly describe the problem here.

Constraints:
- Add constraints here.

Examples:
- Input:
- Output:
"""

from level.domains.practice.harness import run_interview

def solve(*args):
    """
    Implement your solution here.

    Args:
        Define parameters clearly.

    Returns:
        Expected return value.
    """
    raise NotImplementedError("Implement the solution")


def main():
    phases = [
        (
            "Base problem",
            [
                # (input_args_tuple, expected_output, message)
                # ((...), ..., "Simplest example"),
                # ((...), ..., "Simple example"),
                (([],), [], "Empty input so tests fail until you implement the solution"),
            ],
        ),
        (
            "Twist 1",
            [
                # Add new constraints here after base problem passes
            ],
        ),
        (
            "Twist 2",
            [
                # Harder constraint or performance requirement
            ],
        ),
    ]

    followups = [
        # ("Follow-up question", "Hint or guidance for the follow-up"),
    ]

    run_interview(solve, phases, followups=followups)

if __name__ == "__main__":
    main()
