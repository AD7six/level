"""
${TITLE}

This file defines the interview problem.

Instructions for the problem author:

1. Define the base problem clearly through test cases.
2. Each additional phase must:
   - Introduce new reasoning,
   - Break a naive solution,
   - Increase constraints or change assumptions.
3. Remove this header once the problem is fully specified.
"""

# PHASE STRUCTURE:
# Each phase is:
# (
#     "Phase name",
#     [
#         (input_args_tuple, expected_output, description),
#     ],
# )

PHASES = [
    (
        "Base problem",
        [
            # Replace these placeholder cases.
            # Example format:
            # (([1, 2, 3],), 6, "Basic example"),
            ((None,), None, "Placeholder test — replace with real problem"),
        ],
    ),
    (
        "Twist 1 – edge cases",
        [
            # Add cases that break naive implementations.
        ],
    ),
    (
        "Twist 2 – performance or constraint shift",
        [
            # Add large input or stricter constraints.
        ],
    ),
]

FOLLOWUPS = [
    # Add conceptual interview follow-up questions here.
    # ("What is the time complexity?", None),
    # ("How would you optimize this?", None),
]
